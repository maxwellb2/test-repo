{
  "name": "hilbert-ring",
  "private": true,
  "description": "Ten-million-node connected degree-2 Hilbert ring in one packed WebGL buffer.",
  "scripts": {
    "build": "browserify index.js > bundle.js",
    "start": "python3 -m http.server 8080",
    "test": "node test/hilbert.test.js",
    "memory": "node --expose-gc test/memory.js"
  },
  "dependencies": {
    "ngraph.graph": "^0.0.12",
    "ngraph.pixel": "^2.4.1"
  },
  "devDependencies": {
    "browserify": "^9.0.8"
  }
}
